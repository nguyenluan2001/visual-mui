import { Box } from '@mui/material';
import React from 'react';

function Header() {
  return (
    <Box sx={{ height: '100%', width: '100vw', bgcolor: 'blue' }}>Header</Box>
  );
}

export default Header;
